module Reg5Bit(Din,ld,clk,rst,Dout);

	input Din,clk,rst,ld;
	output Dout;
	reg [4:0] Dout;

	always @(posedge clk)
	begin
		if(rst)
			Dout <= 5'b00000;
		else if (ld)
			Dout <= Din;
	end
endmodule
